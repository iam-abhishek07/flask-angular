
export interface Schema {
    
    defaults?: boolean;
    
    dryRun?: boolean;
    
    force?: boolean;
    
    help?: HelpUnion;
    
    interactive?: boolean;
    
    schematic?: string;
}

export declare type HelpUnion = boolean | HelpEnum;
export declare enum HelpEnum {
    HelpJson = "JSON",
    Json = "json"
}
